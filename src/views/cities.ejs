<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <%- include('includes/head') %>
</head>

<body>
    <div class="loader-page"></div>
    <%- include('includes/nav') %>

    <div class="pt-2 pb-3">
        <label class="form-label my-auto mx-0 px-3" for="country">Seleccione un país</label>
        <div class="px-3 w-100">
            <select class="form-select text-primary border-primary" aria-label="Seleccione una opción" name="country" id="country" onchange="specifyCountry(this.value)">
                <option value="">Seleccione una opción</option>
                <%- include('includes/countrySelector') %>
            </select>
        </div>
    </div>

    <div class="container-fluid px-2 px-md-5 py-3"  style="background-color: #e6e6e6;">
        <div class="text-center">
            <h1 class="display-3 text-primary">Ciudades con redes de apoyos</h1>
            <p class="lead fw-normal">
                <% if (data.length !== 0 && country !== "") {%>
                    <i class="bi bi-search text-primary"></i> Selecciona una opción <i class="bi bi-search text-primary"></i>
                <% }else if (data.length === 0 && country !== "") { %>
                    <i class="bi bi-exclamation-triangle text-primary"></i> No se encontraron ciudades con redes de apoyo en el país seleccionado. Por favor, seleccione otro país <i class="bi bi-exclamation-triangle text-primary"></i>
                <% }else{ %>
                    <i class="bi bi-info-square text-primary"></i> Debe de seleccionar un país para poder ver las ciudades con redes locales disponible <i class="bi bi-info-square text-primary"></i>
                <% } %>
            </p>
        </div>

        <div class="row justify-content-center">
            <% if (data.length !== 0) { for (const element of data[0].cities) { %>
            <div class="col-12 col-md-6 col-lg-4 py-2">
                <div class="card h-100">
                    <div class="card-body text-center">
                        <i class="text-primary display-1 bi bi-buildings"></i>
                        <h5 class="card-title"><%= element.name %></h5>
                        <a href="#<%=element.id%>" class="card-link icon-link fw-semibold">
                            Ver <i class="bi bi-arrow-right"></i>
                        </a>
                    </div>
                </div>
            </div>
            <% }} %>
        </div>
    </div>

    <%- include('includes/footer') %>
    <script>
        document.getElementById("country").value = "<%= country %>"
        function specifyCountry(val) {
            window.location.href = `/cities/${val}`
        }
    </script>
    <script src="/js/loader.js"></script>
</body>

</html>